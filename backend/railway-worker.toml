[build]
builder = "nixpacks"

[build.nixpacks]
pkgs = ["nodejs_20"]

[deploy]
startCommand = "node dist/worker.js"
# No healthcheck - workers don't serve HTTP
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
