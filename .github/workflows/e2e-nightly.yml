name: E2E Nightly

on:
  schedule:
    - cron: '0 3 * * *'  # 3am UTC daily
  workflow_dispatch:

jobs:
  e2e-full:
    name: E2E Full Suite (Shard ${{ matrix.shard }}/4)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        shard: [1, 2, 3, 4]
    # ... (copy same setup as main CI)
    steps:
      - run: echo "Full suite runs here"
